/**
 * @name CustomTheme
 * @author YourName
 * @description A clean, customizable Discord theme
 * @version 1.0
 */

/* Import external resources, like fonts or additional CSS if needed */
@import url('https://fonts.googleapis.com/css2?family=YourFontChoice&display=swap');

:root {
    /* --- User variables --- */

    /* Background image (optional) */
    --background-tertiary-alt: url("YourBackgroundImageURL");

    /* Border-radius for the entire theme */
    --border-radius: 20px;

    /* Colors */
    --text-normal: #ffffff;  /* Default text color */
    --text-muted: #b0b0b0;   /* Muted text color */
    --interactive-normal: #ffa7c4;  /* Button and interactive elements */
    --interactive-hover: #ff85a6;   /* Hover state */
    --interactive-active: #ff6589;  /* Active state */
    --background-primary: #121212;  /* Main background */
    --background-secondary: #1e1e1e; /* Secondary background */
    --background-floating: #2c2c2c; /* Floating elements like modals */
    --scrollbar-auto-thumb: #5c5c5c; /* Scrollbar */
    --scrollbar-auto-track: transparent;
}

body {
    font-family: 'YourFontChoice', sans-serif;
}

/* General theme adjustments */
.theme-dark {
    --background-primary: #181818;
    --background-secondary: #222222;
    --text-normal: #f0f0f0;
}

/* Channel list and sidebar */
.sidebar-2K8pFh {
    background-color: var(--background-secondary);
}

.sidebar-2K8pFh .container-2cd8Mz {
    color: var(--text-normal);
}

/* Chat area */
.chat-3bRxxu {
    background-color: var(--background-primary);
    color: var(--text-normal);
}

/* Message area */
.message-2qnXI6 {
    background-color: var(--background-secondary);
    border-radius: var(--border-radius);
    color: var(--text-normal);
}

/* Scrollbar styling */
::-webkit-scrollbar-thumb {
    background-color: var(--scrollbar-auto-thumb);
}

/* Button styles */
.lookFilled-1Gx00P {
    background-color: var(--interactive-normal);
}

.lookFilled-1Gx00P:hover {
    background-color: var(--interactive-hover);
}

/* Additional Background Customizations */
:root .app-2CXKsg::before {
    background: var(--background-tertiary-alt);
}

/* Remove unwanted content */
:root .app-2CXKsg::after,
:root .layers-OrUESM::after {
    content: none !important;
}
